- name: Create Knowlage Directory in not exist
  include_tasks: subplaybooks/create_knowlage_dir.yaml

- name: Clone hacktricks
  become_user: "{{ kali_username }}"
  git:
    repo: https://github.com/carlospolop/hacktricks.git
    depth: 1
    clone: true
    force: true
    dest: "{{ knowlage_directory }}/hacktricks"